import SwiftUI

struct ParticleBackground: View {
    var body: some View {
        Canvas { context, size in
            // Placeholder for ambient particle animation
        }
        .ignoresSafeArea()
    }
}
